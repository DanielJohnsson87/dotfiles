# Feature Plans

Active and past feature plans are stored in `~/plans/<feature-name>/`.

When resuming work on a feature, or when asked to get up to speed on a feature, read the plan artifacts in the relevant `~/plans/<feature-name>/` directory. Start with the most recent stage file and work backwards as needed.

To list all features: `ls ~/plans/`
